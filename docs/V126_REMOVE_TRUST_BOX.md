# V126 — حذف صندوق "مسار أوضح للفرص"

## التغيير
- حذف صندوق `footer-trust-box-v85` بالكامل من `components/Footer.jsx` (العنوان "مسار أوضح للفرص" والنص "تصفح، حفظ، مشاركة...").
- تعديل `.footer-top-v85` من شبكة بعمودين إلى `display:block` لأن الصف صار يحتوي عنصرًا واحدًا (صندوق العلامة فقط) — لمنع بقاء نصف فارغ.

## ملاحظة تنظيف
- تنسيقات `.footer-trust-box-v85` في globals.css أصبحت الآن CSS ميت (لا عنصر يحملها). تُركت كما هي في هذا الإصدار؛ يمكن حذفها لاحقًا ضمن تدقيق تنظيف منفصل عند الرغبة.
