# V134 — إضافة قسم المدونة (مقالات بملفات في الكود)

## الملفات الجديدة
- lib/blogPosts.js — بيانات المقالات.
- app/blog/page.jsx — صفحة قائمة المقالات.
- app/blog/[slug]/page.jsx — صفحة المقال الواحد (SEO + generateStaticParams).
- تنسيقات CSS (كتلة v1.34) في globals.css.

## الربط
"المدونة" رابط مستقل في الهيدر العلوي + القائمة الجانبية + الفوتر.

## إضافة مقال جديد
افتح lib/blogPosts.js، انسخ كائن مقال موجود وضعه أعلى المصفوفة، وعدّل قيمه.
المحتوى كتل: paragraph / heading / list / quote.
